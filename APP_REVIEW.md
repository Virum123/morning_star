# Morning Star 앱 구조 및 사용성 리뷰

검토일: 2026-05-11  
검토 범위: `main.py`, `build_mac_app.py`, `install_desktop_launcher.py`, `ui/src/App.jsx`, `ui/src/components/*`, `ui/src/utils/*`, README

## 1. 앱 구조

Morning Star는 Python 호스트와 React UI가 결합된 로컬 데스크톱 앱이다.

- 호스트/백엔드: `main.py`
  - `pywebview`로 React UI를 데스크톱 창에 띄운다.
  - `Api` 클래스가 프론트엔드에서 호출하는 파일 읽기/쓰기, 설정 저장, 날짜 마이그레이션, 휴지통, 자주 하는 일 기능을 담당한다.
  - 데이터는 macOS 기준 `~/Library/Application Support/MorningStar`에 저장된다.

- 프론트엔드: `ui/src`
  - `App.jsx`: 앱 셸, 사이드바, 테마, 좌상단 동적 아이콘/인사말, 라우팅 역할.
  - `Planner.jsx`: 일간/주간/월간 플래너 컨테이너.
  - `DailyPlanner.jsx`: 오늘/내일/과거 날짜의 핵심 작업 UI, 체크/편집/삭제/드래그/자주 하는 일/어제 못한 일.
  - `WeeklyPlanner.jsx`: 주 단위 작업 보기와 날짜 간 드래그 이동.
  - `MonthlyPlanner.jsx`: 월간 요약과 날짜 점프.
  - `Archive.jsx`: 지난 일정과 변경 기록.
  - `Settings.jsx`: 목표 시간, 테마, 언어, 프로필, 휴지통.
  - `utils/api.js`: pywebview API 래퍼와 개발용 mock.
  - `utils/date.js`: 앱 기준 날짜. 새벽 2시 전은 전날로 간주한다.

- 빌드/배포
  - `build_mac_app.py`: UI 빌드, PyInstaller 패키징, macOS `.app` 생성.
  - `install_desktop_launcher.py`: Desktop의 런처 앱을 최신 workspace 앱으로 연결.
  - `.vendor`: macOS 패키징용 Python 의존성이 vendoring되어 있다.

## 2. 핵심 데이터 흐름

앱은 작업을 Markdown 체크리스트 파일로 저장한다. 설정 파일의 `files` 구조가 각 파일이 어떤 날짜/영역에 속하는지 관리한다.

```text
files
  today: []
  tomorrow: []
  yesterday:
    YYYY-MM-DD: []
  trash: []
```

주의할 점은 `yesterday`라는 이름이 실제 의미와 다르게 쓰인다는 점이다. 현재 구조에서는 과거 기록뿐 아니라, 내일 이후의 날짜별 예정 파일도 `yesterday[YYYY-MM-DD]` 아래에 저장된다. 이 명명은 최근 발생한 “내일/미래 일정이 못한 일처럼 보임”, “과거 일정이 오늘로 밀려 들어옴” 같은 혼란의 근본 원인 중 하나다.

현재 보정된 로직은 다음 원칙을 따른다.

- `today`: 앱 기준 오늘의 작업.
- `tomorrow`: 앱 기준 내일의 작업.
- `yesterday[date]`: 오늘/내일이 아닌 날짜별 기록 또는 예정.
- 날짜가 명시된 파일은 파일명 속 날짜를 우선한다.
- 앱을 며칠 건너뛰어 실행해도 예전 `tomorrow` 파일을 무조건 오늘로 올리지 않는다.

## 3. 앱 이용 관점의 비판

앱의 방향은 좋다. Markdown을 로컬 파일로 보존하고, 일간/주간/월간 뷰를 제공하며, 자주 하는 일을 빠르게 넣을 수 있다는 점은 개인 루틴 앱으로 강점이 있다. 그러나 현재 사용 경험은 “자동으로 뭔가 해준다”와 “내가 직접 조작한다”의 경계가 흐리다.

가장 큰 문제는 날짜 자동 처리의 설명 부족이다. 앱은 새벽 2시 전을 전날로 간주하므로, 설정 화면에서 이 규칙을 짧게 안내해야 한다. 사용자는 2026-05-11 새벽에 앱을 열었을 때 현실 날짜와 앱 날짜가 다르게 동작하는 이유를 추측하지 않아야 한다.

두 번째 문제는 `오늘`, `내일`, `과거`, `못한 일`, `예정`의 개념이 UI와 저장 구조에서 일관되지 않았던 점이다. 최근 수정으로 동작은 개선됐지만, 코드 내부 명칭이 여전히 `yesterday`에 많은 의미를 담고 있어 앞으로도 기능 추가 시 같은 종류의 회귀가 발생하기 쉽다.

세 번째 문제는 사용자가 파일 단위 상태를 직접 이해해야 하는 순간이 남아 있다는 점이다. “파일명 숨김” 개선은 했지만, 보관함이나 내부 흐름에서는 여전히 파일 중심 모델이 강하다. 사용자는 할 일 앱을 기대하지만, 내부는 파일 관리 앱에 가깝게 움직이는 구간이 있다.

네 번째 문제는 드래그/복사/이동의 의미가 민감하다는 점이다. “어제 못한 일”을 오늘에 추가하는 행위는 사용자의 관점에서 복사인지 이동인지 매우 중요하다. 현재는 오늘로 옮긴 뒤 어제 못한 일 목록에서 빠지도록 정리하는 방향이 사용자 기대에 더 가깝다.

다섯 번째 문제는 빌드 환경이 다소 취약하다는 점이다. 같은 `python3`라도 conda Python으로 빌드하면 PyObjC 문제가 생길 수 있었다. 앱의 제품 품질과는 별개로, 개발/배포 재현성이 낮으면 작은 수정도 위험해진다.

## 4. 이용자 관점의 불편한 점

- 오늘이 비어 있는데 과거 날짜의 일이 갑자기 들어와 있는 것처럼 보이면 신뢰가 크게 떨어진다.
- “어제 못한 일”이 오늘에 추가될 때 원본이 사라지는지 남는지 사용자가 즉시 알기 어렵다.
- 새벽 2시 전에는 앱 기준 날짜와 실제 날짜가 다를 수 있는데, 이 차이를 설정 화면에서 확인할 수 있어야 한다.
- 과거 일정, 예정 일정, 못한 일이 같은 날짜 저장소에서 파생되기 때문에 UI가 조금만 어긋나도 사용자는 데이터가 삭제됐다고 느낀다.
- 주간 드래그 이동은 편리하지만, 드래그가 복사인지 이동인지, 원본 파일이 어떻게 바뀌는지 명시적 피드백이 부족하다.
- 보관함은 날짜별 파일 목록 중심이라 “그날 실제로 무엇을 했고 무엇을 못했는지”를 빠르게 훑기 어렵다.
- 설정의 목표 시간은 알림/표시 시간과 관련되어 있지만, 앱 기준 하루 시작 시간인 새벽 2시와의 관계가 설명되어야 한다.

## 5. 개선 제안

우선순위 높음:

1. 내부 데이터 모델 이름 정리
   - `yesterday`를 `dated` 또는 `byDate`로 바꾸는 것이 좋다.
   - 과거와 미래 예정 모두 날짜 키로 저장하되, UI에서 `past`, `scheduled`, `today`, `tomorrow`로 해석하도록 계층을 분리한다.

2. 날짜 마이그레이션 테스트 추가
   - 1일 경과, 2일 이상 경과, 새벽 2시 전, 파일명에 날짜가 있는 경우, 없는 경우를 fixture로 테스트해야 한다.
   - 이번에 발생한 “5/9 일정이 5/11 오늘로 유입” 같은 회귀를 자동으로 잡을 수 있다.

3. 앱 기준 날짜 안내
   - 플래너 본문에는 노출하지 않고, 설정에 “하루 기준은 새벽 2시에 초기화됩니다.” 같은 짧은 안내가 필요하다.
   - 특히 새벽 시간대에는 혼란을 줄이는 효과가 크다.

4. 복사/이동 동작 단순화
   - “어제 못한 일”에서 오늘로 가져오면 오늘 일정에 추가하고, 어제 못한 일 목록에서는 바로 빠지게 하는 편이 직관적이다.
   - 화면에 별도 성공 문구를 띄우기보다 목록 변화 자체로 결과를 보여주는 편이 덜 방해된다.

5. 빌드 Python 고정
   - macOS 빌드는 Xcode Python 또는 지정된 Python 경로를 강제하는 편이 안전하다.
   - `python3 build_mac_app.py` 대신 `run_mac_build.sh` 같은 단일 진입점을 만들고, conda Python 감지 시 경고하는 것이 좋다.

우선순위 중간:

6. 보관함을 파일 목록이 아니라 작업 요약 중심으로 개선
   - 날짜별 완료/미완료 개수, 못한 일 목록, 완료 목록을 먼저 보여주고 파일 미리보기는 보조로 둔다.

7. 일간/주간/월간의 날짜 해석 공통화
   - 현재 각 컴포넌트가 `todayStr`, `tomorrowStr`, `filesData.yesterday[date]` 해석을 반복한다.
   - `getFilesForDate(dateStr, filesData, appDateContext)` 같은 공통 유틸로 묶는 것이 안전하다.

8. 작업 조작 로그
   - 자동 마이그레이션, 복사, 삭제, 휴지통 이동, 복원 같은 이벤트를 간단히 기록하면 “왜 사라졌지?”를 추적하기 쉽다.

9. 빈 상태 UX 개선
   - 오늘 할 일이 비었을 때 “정말 할 일이 없음”과 “날짜 보정으로 이동됨”은 다르다.
   - 별도 성공 문구를 띄우지 않고도 빈 상태 문구와 목록 변화가 명확하게 보이도록 다듬는 편이 좋다.

10. 개발용 mock 데이터 최신화
   - `api.js`의 dev mock은 현재 실제 데이터 구조와 완전히 같은 동작을 보장하지 않는다.
   - 날짜별 예정, 과거 기록, trash를 포함한 mock으로 확장하는 것이 좋다.

## 6. 현재 상태에 대한 판단

현재 앱은 개인 루틴/플래너 앱으로서 핵심 기능은 갖춰져 있다. 하지만 사용자의 신뢰가 중요한 날짜/데이터 이동 로직이 아직 복잡하고, 내부 명칭이 실제 의미와 다르다. 기능을 더 추가하기 전에 날짜 모델과 마이그레이션 테스트를 안정화하는 것이 가장 효율적이다.

단기적으로는 지금 수정된 날짜 보정 로직을 유지하면서, 설정에서 앱 기준 날짜 규칙을 짧게 안내하는 것이 좋다. 장기적으로는 `today/tomorrow/yesterday` 중심 구조에서 `byDate` 중심 구조로 전환하는 것이 맞다.

## 7. 다음 작업 제안

- `files.yesterday`를 `files.byDate`로 마이그레이션하는 설계안 작성.
- 날짜 마이그레이션 테스트 스크립트 추가.
- “어제 못한 일 → 오늘로 복사” 토스트/상태 메시지 추가.
- 보관함을 작업 요약 중심으로 재구성.
- macOS 빌드 Python 고정 및 conda 환경 감지 경고 추가.
